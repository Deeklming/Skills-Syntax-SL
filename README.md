# SkillsSyntaxSTL

```
MAINLANG => c++, rust, c, c#, 
SUBLANG => go, javascript, python, dart, shell, 
DEFERLANG => asm, typescript, swift, kotlin, objective-c, java,

SKILL => socket, rocky, debian, windows, nodejs, mariadb, mongodb, postgresql, 
DEFERSKILL => boost, regex, fastapi, docker, macos, bootstrap, svelte, django, flutter, ios, android

MATH => 기초수학, 선형대수학, 기초해석학, 확률과통계, 수치해석학, 이산수학, 정수론, 추상대수학, 암호학
SCIENCE => 물리학
LICENSE => 정처기, 정보기, 빅분기, 지텔프

NOW => {
    skill -> [network programming]
    algorithm -> []
}
```

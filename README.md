# SkillsSyntaxSTL

```
MAINLANG => go, rust, c, python, 
SUBLANG => c++, javascript, 
DEFERLANG => c#, asm, typescript, shell, kotlin, swift, 

SKILL => vscode, mark, bootstrap, postgresql, 
DEFERSKILL => nodejs, android, ios, linux, mariadb, mongodb, dbeaver, boost, svelte, express, redmine, regex, algorithm, fastapi, django, git, docker, windows, macos, freemarker, java, 

MATH => 기본, 선형대수학, 기초해석학, 확률과통계, 수치해석학, 이산수학, 정수론, 추상대수학, 암호학
ENGLISH => 영문법, 영인강

LICENSE => 정보기, 정처기, 빅분기, 지텔프

PROJECT => Performance Monitoring of Service(OTT+ShopingMall)

NOW => {
    syntax -> [go, c++, python]
    skill -> [nodejs, svelte]
    algorithm -> [c++ python]
    math -> [기본수학, 기본통계, 선형대수학]
    project -> [PMoS]
    license -> [정처기, 정보기, 빅분기, 지텔프]
}
```

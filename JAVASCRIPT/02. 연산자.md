# 연산자

## 연산자들
```
복합할당연산자
 =, +=, -=, *=, /=, %=, **=(지수할당)
비교연산자 - 문자열 비교는 유니코드를 써서 사전 순으로 비교한다
 ==, !=, ===, !==, >, >=, <, <=
산술연산자
 +, -(연산), *, /, %, expr++, ++expr, expr--, --expr, -(단항, 부호바꿈), +(단항, 숫자가 아닌 타입을 숫자로 변환 시도 함)
논리연산자
 &&, ||, !
조건(삼항)연산자
조건식? 참:거짓
```
